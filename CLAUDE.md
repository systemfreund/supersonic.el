## Agent skills

### Issue tracker

Issues live as GitHub issues on `systemfreund/supersonic.el`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
